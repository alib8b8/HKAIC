"""
HKC 攻击场景库 (attack_scenarios.py)
======================================
链上安全对战的红队武器库。
覆盖主流链上攻击类型，每种攻击有强度、持续时间和自动化脚本。
支持攻击组合生成复合场景，攻击评分基于创新性和威胁程度。

攻击类型分类：
  - 共识层：双花攻击、51%攻击、自私挖矿
  - 网络层：女巫攻击、P2P日食攻击、DDoS
  - 交易层：交易重放、三明治攻击、闪电贷攻击
  - 合约层：合约重入、整数溢出、预言机操纵
  - 跨链层：跨链桥攻击
  - 涌现层：涌现分数操纵、协同图攻击（HKC特有）

纯Python标准库，零外部依赖。
"""

import hashlib
import math
import os
import time
import copy
from dataclasses import dataclass, field
from typing import Dict, List, Optional, Tuple
from enum import Enum


class 攻击类型(Enum):
    """攻击类型枚举"""
    # 共识层
    双花攻击 = "double_spend"
    百分之51攻击 = "51_percent"
    自私挖矿 = "selfish_mining"
    # 网络层
    女巫攻击 = "sybil"
    P2P日食攻击 = "eclipse"
    DDoS攻击 = "ddos"
    # 交易层
    交易重放 = "replay"
    三明治攻击 = "sandwich"
    闪电贷攻击 = "flash_loan"
    # 合约层
    合约重入 = "reentrancy"
    整数溢出 = "integer_overflow"
    预言机操纵 = "oracle_manipulation"
    # 跨链层
    跨链桥攻击 = "bridge_attack"
    # 涌现层（HKC特有）
    涌现分数操纵 = "emergence_manipulation"
    协同图攻击 = "synergy_graph_attack"


class 攻击层级(Enum):
    """攻击影响的层级"""
    共识层 = "consensus"
    网络层 = "network"
    交易层 = "transaction"
    合约层 = "contract"
    跨链层 = "crosschain"
    涌现层 = "emergence"


@dataclass
class 攻击参数:
    """攻击参数配置"""
    强度: int = 5                  # 1-10，攻击强度
    持续时间秒: float = 60.0       # 攻击持续时间
    目标范围: float = 0.5          # 0-1，攻击覆盖的目标比例
    资源限制: float = 1000.0       # 攻击可用资源（模拟Gas/计算力）
    自适应: bool = True            # 是否根据防御自适应调整


@dataclass
class 攻击场景:
    """单个攻击场景定义"""
    场景ID: str
    类型: 攻击类型
    层级: 攻击层级
    名称: str
    描述: str
    参数: 攻击参数 = field(default_factory=攻击参数)
    威胁等级: int = 5              # 1-10，预设威胁等级
    创新性: float = 0.5            # 0-1，攻击创新性
    前置条件: List[str] = field(default_factory=list)  # 前置条件描述
    攻击步骤: List[str] = field(default_factory=list)  # 攻击步骤描述
    检测难度: float = 0.5          # 0-1，被检测的难度

    def 攻击评分(self) -> float:
        """计算攻击评分——基于创新性和威胁程度

        评分 = 威胁等级 × 10 × (0.5 + 创新性 × 0.5)
        高创新性攻击评分更高，鼓励发现新攻击模式
        """
        基础分 = self.威胁等级 * 10
        创新加成 = 0.5 + self.创新性 * 0.5
        强度调整 = self.参数.强度 / 10.0
        return 基础分 * 创新加成 * (0.5 + 强度调整 * 0.5)


class 攻击场景库:
    """
    攻击场景库——红队AI的武器库

    功能：
      - 预设攻击模板：覆盖主流链上攻击类型
      - 攻击组合生成：多攻击组合的复合场景
      - 攻击脚本执行：模拟攻击执行过程
      - 攻击评分：基于创新性和威胁程度
      - 自定义攻击：用户可创建新攻击场景

    与进化沙盒联动：
      - 攻击场景用于变异沙盒的压力测试
      - 新发现的攻击模式自动入库
    """

    def __init__(self):
        """初始化攻击场景库"""
        self._场景池: Dict[str, 攻击场景] = {}
        self._攻击历史: List[dict] = []
        self._涌现攻击发现: List[dict] = []  # 涌现式新攻击发现

        # 加载预设攻击模板
        self._加载预设攻击()

    def _加载预设攻击(self):
        """加载预设攻击场景模板"""
        预设列表 = [
            攻击场景(
                场景ID="atk_double_spend",
                类型=攻击类型.双花攻击,
                层级=攻击层级.共识层,
                名称="双花攻击",
                描述="同一笔资金在不同交易中被花费两次，利用共识延迟窗口",
                参数=攻击参数(强度=7, 持续时间秒=30, 目标范围=0.3),
                威胁等级=8, 创新性=0.3,
                前置条件=["控制一定算力或质押量", "存在共识确认延迟窗口"],
                攻击步骤=[
                    "1. 发起交易A将资金转给受害者",
                    "2. 在确认前发起交易B将同一资金转给自己",
                    "3. 通过算力优势使交易B被确认而非交易A",
                ],
                检测难度=0.4,
            ),
            攻击场景(
                场景ID="atk_51_percent",
                类型=攻击类型.百分之51攻击,
                层级=攻击层级.共识层,
                名称="51%攻击",
                描述="控制超过50%的验证者/算力，篡改链上交易历史",
                参数=攻击参数(强度=10, 持续时间秒=120, 目标范围=1.0, 资源限制=50000),
                威胁等级=10, 创新性=0.1,
                前置条件=["控制>50%验证者或算力"],
                攻击步骤=[
                    "1. 积累足够的验证者或质押量",
                    "2. 在私有分支上构建替代链",
                    "3. 发布更长的替代链替换主链",
                ],
                检测难度=0.3,
            ),
            攻击场景(
                场景ID="atk_sybil",
                类型=攻击类型.女巫攻击,
                层级=攻击层级.网络层,
                名称="女巫攻击",
                描述="创建大量虚假身份，影响投票或共识结果",
                参数=攻击参数(强度=6, 持续时间秒=300, 目标范围=0.5),
                威胁等级=7, 创新性=0.2,
                前置条件=["可创建多个验证者身份"],
                攻击步骤=[
                    "1. 创建大量虚假节点身份",
                    "2. 注册为验证者参与共识",
                    "3. 协同投票操纵共识结果",
                ],
                检测难度=0.5,
            ),
            攻击场景(
                场景ID="atk_replay",
                类型=攻击类型.交易重放,
                层级=攻击层级.交易层,
                名称="交易重放攻击",
                描述="在分叉链或跨链场景下重放有效交易",
                参数=攻击参数(强度=4, 持续时间秒=10, 目标范围=0.2),
                威胁等级=5, 创新性=0.3,
                前置条件=["链存在分叉", "跨链桥缺乏重放保护"],
                攻击步骤=[
                    "1. 监控主链上的有效交易",
                    "2. 在分叉链或目标链上重放相同交易",
                    "3. 重复获取资金或服务",
                ],
                检测难度=0.6,
            ),
            攻击场景(
                场景ID="atk_sandwich",
                类型=攻击类型.三明治攻击,
                层级=攻击层级.交易层,
                名称="三明治攻击",
                描述="在目标交易前后插入交易，通过价格滑点获利",
                参数=攻击参数(强度=5, 持续时间秒=5, 目标范围=0.1),
                威胁等级=6, 创新性=0.4,
                前置条件=["DEX存在可预测的交易排序", "Gas竞价能力"],
                攻击步骤=[
                    "1. 监控mempool发现大额待处理交易",
                    "2. 在目标交易前插入买入交易（推高价格）",
                    "3. 在目标交易后插入卖出交易（获利）",
                ],
                检测难度=0.7,
            ),
            攻击场景(
                场景ID="atk_flash_loan",
                类型=攻击类型.闪电贷攻击,
                层级=攻击层级.交易层,
                名称="闪电贷攻击",
                描述="利用闪电贷无抵押借贷进行价格操纵攻击",
                参数=攻击参数(强度=8, 持续时间秒=15, 目标范围=0.3, 资源限制=100),
                威胁等级=9, 创新性=0.6,
                前置条件=["DEX存在流动性不足的交易对", "预言机依赖DEX价格"],
                攻击步骤=[
                    "1. 通过闪电贷借入巨额资金",
                    "2. 在DEX上大量交易操纵价格",
                    "3. 利用被操纵的预言机价格在借贷协议中获利",
                    "4. 归还闪电贷，净赚差价",
                ],
                检测难度=0.5,
            ),
            攻击场景(
                场景ID="atk_reentrancy",
                类型=攻击类型.合约重入,
                层级=攻击层级.合约层,
                名称="合约重入攻击",
                描述="利用合约外部调用重入漏洞重复提取资金",
                参数=攻击参数(强度=7, 持续时间秒=30, 目标范围=0.1),
                威胁等级=8, 创新性=0.3,
                前置条件=["目标合约存在状态更新后外部调用的漏洞"],
                攻击步骤=[
                    "1. 调用目标合约的提款函数",
                    "2. 在回调函数中重新调用提款函数",
                    "3. 合约状态未更新导致可重复提取",
                ],
                检测难度=0.4,
            ),
            攻击场景(
                场景ID="atk_integer_overflow",
                类型=攻击类型.整数溢出,
                层级=攻击层级.合约层,
                名称="整数溢出攻击",
                描述="利用合约算术运算溢出漏洞进行非法操作",
                参数=攻击参数(强度=5, 持续时间秒=5, 目标范围=0.05),
                威胁等级=6, 创新性=0.2,
                前置条件=["目标合约使用未检查的算术运算"],
                攻击步骤=[
                    "1. 向合约发送极大数值触发溢出",
                    "2. 溢出后数值绕回产生异常结果",
                    "3. 利用异常结果非法获利",
                ],
                检测难度=0.6,
            ),
            攻击场景(
                场景ID="atk_oracle",
                类型=攻击类型.预言机操纵,
                层级=攻击层级.合约层,
                名称="预言机操纵攻击",
                描述="操纵预言机价格数据源导致合约决策错误",
                参数=攻击参数(强度=8, 持续时间秒=60, 目标范围=0.3),
                威胁等级=9, 创新性=0.5,
                前置条件=["合约依赖单一或少数预言机数据源"],
                攻击步骤=[
                    "1. 在预言机数据源交易所上操纵价格",
                    "2. 被操纵价格被预言机采信",
                    "3. 合约基于错误价格执行交易",
                ],
                检测难度=0.4,
            ),
            攻击场景(
                场景ID="atk_bridge",
                类型=攻击类型.跨链桥攻击,
                层级=攻击层级.跨链层,
                名称="跨链桥攻击",
                描述="利用跨链桥的验证漏洞或签名机制缺陷进行攻击",
                参数=攻击参数(强度=9, 持续时间秒=180, 目标范围=0.5, 资源限制=10000),
                威胁等级=10, 创新性=0.4,
                前置条件=["跨链桥存在验证组漏洞", "或签名机制可被绕过"],
                攻击步骤=[
                    "1. 分析跨链桥验证机制",
                    "2. 伪造跨链消息或绕过验证",
                    "3. 在目标链上提取未授权资产",
                ],
                检测难度=0.3,
            ),
            攻击场景(
                场景ID="atk_eclipse",
                类型=攻击类型.P2P日食攻击,
                层级=攻击层级.网络层,
                名称="P2P日食攻击",
                描述="将目标节点从网络中隔离，使其只连接攻击者控制的节点",
                参数=攻击参数(强度=6, 持续时间秒=600, 目标范围=0.1),
                威胁等级=7, 创新性=0.4,
                前置条件=["可控制大量网络节点", "目标节点连接数有限"],
                攻击步骤=[
                    "1. 占据目标节点的所有连接槽",
                    "2. 过滤目标节点收到的信息",
                    "3. 喂给目标节点伪造的区块或交易",
                ],
                检测难度=0.6,
            ),
            攻击场景(
                场景ID="atk_ddos",
                类型=攻击类型.DDoS攻击,
                层级=攻击层级.网络层,
                名称="DDoS攻击",
                描述="通过大量请求淹没节点，使其无法正常服务",
                参数=攻击参数(强度=7, 持续时间秒=300, 目标范围=0.8),
                威胁等级=6, 创新性=0.1,
                前置条件=["大量僵尸网络或计算资源"],
                攻击步骤=[
                    "1. 向目标节点发送海量请求",
                    "2. 耗尽目标节点计算和带宽资源",
                    "3. 目标节点无法处理正常交易",
                ],
                检测难度=0.7,
            ),
            攻击场景(
                场景ID="atk_selfish_mining",
                类型=攻击类型.自私挖矿,
                层级=攻击层级.共识层,
                名称="自私挖矿",
                描述="有策略地隐藏已挖出的区块，以获取更高收益",
                参数=攻击参数(强度=5, 持续时间秒=600, 目标范围=0.2),
                威胁等级=5, 创新性=0.3,
                前置条件=["控制一定比例的算力/验证权"],
                攻击步骤=[
                    "1. 挖出区块后不立即广播",
                    "2. 在私有分支上继续挖矿",
                    "3. 选择性发布区块使其他矿工浪费算力",
                ],
                检测难度=0.5,
            ),
            # HKC特有：涌现层攻击
            攻击场景(
                场景ID="atk_emergence_manipulation",
                类型=攻击类型.涌现分数操纵,
                层级=攻击层级.涌现层,
                名称="涌现分数操纵",
                描述="通过伪造协同关系虚增涌现分数E_i，骗取出块权",
                参数=攻击参数(强度=8, 持续时间秒=120, 目标范围=0.3),
                威胁等级=9, 创新性=0.8,
                前置条件=["可创建多个节点", "可伪造协同记录"],
                攻击步骤=[
                    "1. 创建多个节点组成协同环",
                    "2. 节点间互相记录虚假协同关系",
                    "3. 虚增σ_i从而虚增E_i",
                    "4. 骗取高出块概率",
                ],
                检测难度=0.3,
            ),
            攻击场景(
                场景ID="atk_synergy_graph",
                类型=攻击类型.协同图攻击,
                层级=攻击层级.涌现层,
                名称="协同图攻击",
                描述="操纵协同图的拓扑结构，使特定节点获得不成比例的涌现分数",
                参数=攻击参数(强度=7, 持续时间秒=180, 目标范围=0.4),
                威胁等级=8, 创新性=0.9,
                前置条件=["理解PoEI涌现分数计算机制", "可影响协同图结构"],
                攻击步骤=[
                    "1. 分析当前协同图拓扑",
                    "2. 将攻击节点定位为高中心度枢纽",
                    "3. 使多个节点仅与攻击节点协同",
                    "4. 攻击节点获得不成比例的σ_i增益",
                ],
                检测难度=0.2,
            ),
        ]

        for 场景 in 预设列表:
            self._场景池[场景.场景ID] = 场景

    def 创建攻击场景(self, 类型: 攻击类型, 名称: str, 描述: str,
                       参数: Optional[攻击参数] = None,
                       威胁等级: int = 5, 创新性: float = 0.5,
                       攻击步骤: Optional[List[str]] = None) -> 攻击场景:
        """创建自定义攻击场景

        参数:
            类型: 攻击类型
            名称: 攻击名称
            描述: 攻击描述
            参数: 攻击参数
            威胁等级: 1-10
            创新性: 0-1
            攻击步骤: 步骤列表
        返回:
            新创建的攻击场景
        """
        # 确定攻击层级
        层级映射 = {
            攻击类型.双花攻击: 攻击层级.共识层,
            攻击类型.百分之51攻击: 攻击层级.共识层,
            攻击类型.自私挖矿: 攻击层级.共识层,
            攻击类型.女巫攻击: 攻击层级.网络层,
            攻击类型.P2P日食攻击: 攻击层级.网络层,
            攻击类型.DDoS攻击: 攻击层级.网络层,
            攻击类型.交易重放: 攻击层级.交易层,
            攻击类型.三明治攻击: 攻击层级.交易层,
            攻击类型.闪电贷攻击: 攻击层级.交易层,
            攻击类型.合约重入: 攻击层级.合约层,
            攻击类型.整数溢出: 攻击层级.合约层,
            攻击类型.预言机操纵: 攻击层级.合约层,
            攻击类型.跨链桥攻击: 攻击层级.跨链层,
            攻击类型.涌现分数操纵: 攻击层级.涌现层,
            攻击类型.协同图攻击: 攻击层级.涌现层,
        }

        场景ID = hashlib.sha256(
            f"custom_atk:{名称}:{os.urandom(16).hex()}".encode()
        ).hexdigest()[:24]

        场景 = 攻击场景(
            场景ID=场景ID,
            类型=类型,
            层级=层级映射.get(类型, 攻击层级.交易层),
            名称=名称,
            描述=描述,
            参数=参数 or 攻击参数(),
            威胁等级=威胁等级,
            创新性=创新性,
            攻击步骤=攻击步骤 or [],
        )

        self._场景池[场景ID] = 场景
        return 场景

    def 获取场景(self, 场景ID: str) -> Optional[攻击场景]:
        """获取指定攻击场景"""
        return self._场景池.get(场景ID)

    def 按类型查询(self, 类型: 攻击类型) -> List[攻击场景]:
        """按攻击类型查询场景"""
        return [s for s in self._场景池.values() if s.类型 == 类型]

    def 按层级查询(self, 层级: 攻击层级) -> List[攻击场景]:
        """按攻击层级查询场景"""
        return [s for s in self._场景池.values() if s.层级 == 层级]

    def 随机选择(self, 数量: int = 1, 最低威胁: int = 1) -> List[攻击场景]:
        """随机选择攻击场景

        参数:
            数量: 选择数量
            最低威胁: 最低威胁等级
        """
        候选 = [s for s in self._场景池.values() if s.威胁等级 >= 最低威胁]
        if not 候选:
            return []

        # 使用加密随机数选择
        选中 = []
        剩余 = list(候选)
        for _ in range(min(数量, len(剩余))):
            idx = int.from_bytes(os.urandom(4), 'big') % len(剩余)
            选中.append(剩余.pop(idx))

        return 选中

    def 生成组合攻击(self, 场景列表: List[攻击场景]) -> 攻击场景:
        """生成组合攻击——多种攻击组合的复合场景

        参数:
            场景列表: 要组合的攻击场景列表
        返回:
            组合攻击场景
        """
        if not 场景列表:
            raise ValueError("至少需要一个攻击场景")

        if len(场景列表) == 1:
            return 场景列表[0]

        # 组合属性取各场景的最大/加权值
        名称 = " + ".join(s.名称 for s in 场景列表)
        描述 = f"组合攻击: {'; '.join(s.描述 for s in 场景列表)}"
        最大强度 = max(s.参数.强度 for s in 场景列表)
        总威胁 = min(10, int(sum(s.威胁等级 for s in 场景列表) / len(场景列表) * 1.2)) if len(场景列表) > 1 else 场景列表[0].威胁等级
        平均创新 = sum(s.创新性 for s in 场景列表) / len(场景列表)
        最低检测难度 = min(s.检测难度 for s in 场景列表) * 0.7  # 组合更难检测

        所有步骤 = []
        for s in 场景列表:
            所有步骤.extend(s.攻击步骤)

        组合参数 = 攻击参数(
            强度=最大强度,
            持续时间秒=sum(s.参数.持续时间秒 for s in 场景列表),
            目标范围=min(1.0, sum(s.参数.目标范围 for s in 场景列表)),
            资源限制=sum(s.参数.资源限制 for s in 场景列表),
        )

        场景ID = hashlib.sha256(
            f"combo_atk:{名称}:{os.urandom(16).hex()}".encode()
        ).hexdigest()[:24]

        组合场景 = 攻击场景(
            场景ID=场景ID,
            类型=场景列表[0].类型,  # 主类型取第一个
            层级=场景列表[0].层级,
            名称=名称,
            描述=描述,
            参数=组合参数,
            威胁等级=总威胁,
            创新性=min(1.0, 平均创新 * 1.2),  # 组合攻击创新性加成
            攻击步骤=所有步骤,
            检测难度=最低检测难度,
        )

        self._场景池[场景ID] = 组合场景
        return 组合场景

    def 记录攻击执行(self, 场景ID: str, 攻击者: str, 成功: bool,
                       详情: str = ""):
        """记录攻击执行历史"""
        self._攻击历史.append({
            "场景ID": 场景ID,
            "攻击者": 攻击者,
            "成功": 成功,
            "详情": 详情,
            "时间": time.time(),
        })

    def 发现涌现攻击(self, 名称: str, 描述: str, 攻击步骤: List[str],
                       创新性: float = 0.8) -> 攻击场景:
        """发现新的涌现式攻击模式

        涌现奖励机制：发现全新的攻击模式（特别是涌现层攻击）
        可获得额外HKAIC奖励。

        参数:
            名称: 攻击名称
            描述: 攻击描述
            攻击步骤: 步骤列表
            创新性: 创新性评分
        返回:
            新创建的攻击场景
        """
        场景 = self.创建攻击场景(
            类型=攻击类型.涌现分数操纵,  # 涌现攻击默认类型
            名称=名称,
            描述=描述,
            威胁等级=7,
            创新性=创新性,
            攻击步骤=攻击步骤,
        )

        self._涌现攻击发现.append({
            "场景ID": 场景.场景ID,
            "名称": 名称,
            "创新性": 创新性,
            "发现时间": time.time(),
        })

        return 场景

    def 场景库摘要(self) -> dict:
        """获取场景库摘要"""
        各层级 = {}
        各类型 = {}
        for s in self._场景池.values():
            各层级[s.层级.value] = 各层级.get(s.层级.value, 0) + 1
            各类型[s.类型.value] = 各类型.get(s.类型.value, 0) + 1

        return {
            "总场景数": len(self._场景池),
            "层级分布": 各层级,
            "类型分布": 各类型,
            "攻击历史": len(self._攻击历史),
            "涌现发现": len(self._涌现攻击发现),
        }
